# ===========================
# USER PARAMETERS
# ===========================

MIN_READS        <- 5000
RARE_DEPTH       <- 5000
N_RAREFACTIONS   <- 20 
MAX_ABSENCES     <- 3 #occupancy based on sample count
#CORE_THRESHOLD   <- 0.95 #occupancy thresholds
RANDOM_SEED      <- 1234
MIN_COUNT        <- 1
MIN_REL_ABUND    <- 0.0005

